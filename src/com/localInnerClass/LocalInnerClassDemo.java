package com.localInnerClass;

public class LocalInnerClassDemo {
static{//here LocalInnerClass is inside the block(using static to create a block)      this also can be written inside the method, or inside the constructor
	static class LocalInnerClass{
		
	}
}
}
